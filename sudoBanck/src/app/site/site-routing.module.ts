import { Routes } from '@angular/router';
import { SiteComponent } from './site.component';

// @NgModule({
//   // imports: [RouterModule.forRoot(routes)],
//   // exports: [RouterModule]
// })
export const SiteRouting: Routes = [
  {
    path: '',
    component: SiteComponent
  }
];
